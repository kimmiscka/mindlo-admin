import { MapPin } from 'lucide-react';
import PlaceholderPage from '../components/PlaceholderPage';

export default function Directory() {
  return (
    <PlaceholderPage
      title="Directory"
      subtitle="Support services and partner organisations"
      icon={MapPin}
      description="Edit helpline numbers, descriptions, and website links for SADAG, Childline, and partner organisations like GYS and NexGen — without a mobile release."
    />
  );
}
